# 后端运行依赖（开发环境与桌面端打包共用）
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic-settings>=2.6
sqlalchemy>=2.0

# 公开数据源（财务/舆情/诉讼）。桌面端打包必需：
# 缺失时打包产物仍可启动，但"刷新数据"会报 ModuleNotFoundError。
akshare>=1.17
